import {Link} from 'react-router-dom';import {Heart} from 'lucide-react';import {useApp} from '../context/AppContext';import ProductCard from '../components/ProductCard';
export default function Wishlist(){const {user,wishlist}=useApp();if(!user)return <div className="empty"><Heart/><h2>Your wishlist awaits</h2><Link className="btn" to="/login">Sign in</Link></div>;return <section className="section"><div className="sectop"><div><span className="eyebrow">SAVED FOR LATER</span><h2>Wishlist</h2></div></div>{wishlist?.products?.length?<div className="grid">{wishlist.products.map(p=><ProductCard key={p._id} p={p}/>)}</div>:<div className="empty"><Heart size={44}/><h3>Nothing saved yet</h3><Link className="btn" to="/shop">Explore products</Link></div>}</section>}
