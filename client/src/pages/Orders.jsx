import {Link,useParams} from 'react-router-dom';import {useEffect,useState} from 'react';import {PackageCheck,ArrowLeft} from 'lucide-react';import api from '../api';import {useApp} from '../context/AppContext';
const steps=['Placed','Processing','Shipped','Out for Delivery','Delivered'];
export function Orders(){const [orders,setOrders]=useState([]);const {user}=useApp();useEffect(()=>{if(user)api.get('/orders/mine').then(r=>setOrders(r.data))},[user]);return <section className="section"><div className="sectop"><div><span className="eyebrow">ACCOUNT</span><h2>Order history</h2></div></div>{orders.length?<div className="orderlist">{orders.map(o=><Link className="orderrow" to={'/orders/'+o._id} key={o._id}><div className="ordericon"><PackageCheck/></div><div><b>Order #{o._id.slice(-7).toUpperCase()}</b><small>{new Date(o.createdAt).toLocaleDateString()} · {o.items.length} items</small></div><span className="status">{o.status}</span><strong>₹{o.total.toFixed(2)}</strong></Link>)}</div>:<div className="empty"><PackageCheck/><h3>No orders yet</h3><Link className="btn" to="/shop">Shop now</Link></div>}</section>}
export function OrderDetail(){const {id}=useParams();const [o,setO]=useState(null);useEffect(()=>{api.get('/orders/'+id).then(r=>setO(r.data))},[id]);if(!o)return <div className="empty">Loading order…</div>;const current=steps.indexOf(o.status);return <section className="section"><Link className="back" to="/orders"><ArrowLeft/> Orders</Link><div className="orderhead"><div><span className="eyebrow">ORDER TRACKING</span><h2>#{o._id.slice(-8).toUpperCase()}</h2><p className="muted">{new Date(o.createdAt).toLocaleString()}</p></div><span className="status">{o.status}</span></div><div className="timeline">{steps.map((s,i)=><div className={i<=current?'step done':'step'} key={s}><div className="dot"></div><b>{s}</b></div>)}</div><div className="orderdetailgrid"><div className="panel"><h3>Items</h3>{o.items.map(x=><div className="miniitem" key={x.product}><img src={x.image}/><span><b>{x.name}</b><small>Qty {x.quantity}</small></span><strong>₹{(x.price*x.quantity).toFixed(2)}</strong></div>)}</div><div className="summary"><h3>Payment & delivery</h3><p>{o.paymentMethod}</p><p>{o.shippingAddress?.name}<br/>{o.shippingAddress?.street}<br/>{o.shippingAddress?.city}, {o.shippingAddress?.state} {o.shippingAddress?.zip}</p><hr/><div><span>Total</span><b>₹{o.total.toFixed(2)}</b></div></div></div></section>}
