import {Link} from 'react-router-dom';import {Heart,ShoppingBag,Star} from 'lucide-react';import {useApp} from '../context/AppContext';
export default function ProductCard({p}){const {addCart,toggleWish,wishlist}=useApp();const liked=wishlist?.products?.some(x=>x._id===p._id);return <article className="pcard"><div className="pimg"><img src={p.images?.[0]} /><button onClick={()=>toggleWish(p._id)} className={liked?'wish liked':'wish'}><Heart size={18} fill={liked?'currentColor':'none'}/></button>{p.stock<5&&<span className="badge">Only {p.stock} left</span>}</div><div className="pbody"><small>{p.category?.name} · {p.brand}</small><Link to={'/product/'+p._id} className="pname">{p.name}</Link><div className="rating"><Star size={15} fill="currentColor"/> {p.rating?.toFixed(1)} <em>({p.reviewCount})</em></div><div className="price">₹{p.price.toFixed(2)} {p.compareAtPrice&&<del>₹{p.compareAtPrice.toFixed(2)}</del>}</div><button className="addbtn" onClick={()=>addCart(p._id)}><ShoppingBag size={17}/> Add to cart</button></div></article>}
