import {Link,useNavigate} from 'react-router-dom';import {ShoppingBag,Heart,UserRound,Search,Menu,X,LogOut,LayoutDashboard} from 'lucide-react';import {useApp} from '../context/AppContext';import {useState} from 'react';
export default function Navbar(){const {user,logout,cart,wishlist}=useApp();const [open,setOpen]=useState(false);const [q,setQ]=useState('');const nav=useNavigate();function submit(e){e.preventDefault();nav('/shop?search='+encodeURIComponent(q));setOpen(false)}
return <header className="nav"><div className="navin"><Link to="/" className="brand">Nexa<span>Cart</span></Link><form className="search" onSubmit={submit}><Search size={18}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search products, brands..." /></form><nav className={open?'links open':'links'}><Link to="/shop" onClick={()=>setOpen(false)}>Shop</Link>{user?.role==='admin'&&<Link to="/admin" onClick={()=>setOpen(false)}><LayoutDashboard size={16}/> Admin</Link>}<Link to="/wishlist" onClick={()=>setOpen(false)}><Heart size={18}/> Wishlist {wishlist?.products?.length>0&&<b>{wishlist.products.length}</b>}</Link><Link to="/cart" onClick={()=>setOpen(false)}><ShoppingBag size={18}/> Cart {cart?.items?.length>0&&<b>{cart.items.length}</b>}</Link>{user?<><Link to="/profile"><UserRound size={17}/>{user.name?.split(' ')[0]}</Link><button className="iconbtn" onClick={logout}><LogOut size={17}/></button></>:<Link className="btn small" to="/login">Sign in</Link>}</nav><button className="mobilebtn" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div></header>}
