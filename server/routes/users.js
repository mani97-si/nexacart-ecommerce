import {Router} from 'express';import {me,updateMe} from '../controllers/userController.js';import {protect} from '../middleware/auth.js';const r=Router();r.use(protect);r.get('/me',me);r.put('/me',updateMe);export default r;