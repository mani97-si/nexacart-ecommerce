import {Router} from 'express';import {stats} from '../controllers/adminController.js';import {allUsers,updateUser,deleteUser} from '../controllers/userController.js';import {protect,adminOnly} from '../middleware/auth.js';const r=Router();r.use(protect,adminOnly);r.get('/stats',stats);r.get('/users',allUsers);r.put('/users/:id',updateUser);r.delete('/users/:id',deleteUser);export default r;